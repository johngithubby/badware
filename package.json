{
  "name": "badware",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "dev:clean": "PORT=${DEV_PORT:-4913}; echo 'Using port '$PORT; (lsof -tiTCP:$PORT -sTCP:LISTEN | xargs -r kill) 2>/dev/null; pkill -f '/node_modules/.bin/astro dev' || true; pkill -f 'astro dev' || true; sleep 0.5; ASTRO_FORCE_COLOR=1 astro dev --port $PORT",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.3.6",
    "@astrojs/rss": "^4.0.12",
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^5.14.1",
    "sharp": "^0.34.3"
  }
}